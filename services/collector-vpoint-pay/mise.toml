# ref: https://mise.jdx.dev/tasks/
#:schema https://mise.jdx.dev/schema/mise.json

[tasks."types"]
description = "Generate the Cloudflare Worker types of services/collector-vpoint-pay."
run = "./node_modules/.bin/wrangler types"

[tasks."typecheck"]
description = "Type-check services/collector-vpoint-pay."
depends = ["//services/collector-vpoint-pay:types"]
run = "./node_modules/.bin/tsc --noEmit"

[tasks."test"]
description = "Run the services/collector-vpoint-pay tests: the pure suite, then the Miniflare R2 suite."
depends = ["//services/collector-vpoint-pay:types"]
run = [
	"bun test ./test/*.test.ts",
	"./node_modules/.bin/vitest run",
]

[tasks."dry-run"]
description = "Validate the services/collector-vpoint-pay deployment without uploading it."
depends = ["//services/collector-vpoint-pay:types"]
run = [
	"./node_modules/.bin/wrangler deploy --dry-run",
]

[tasks."bundle"]
description = "Emit the deployable bundle of services/collector-vpoint-pay for the release manifest."
depends = ["//services/collector-vpoint-pay:types"]
run = "./node_modules/.bin/wrangler deploy --dry-run --outdir ../../dist/vpoint-pay-worker --config wrangler.jsonc"

[tasks."ci"]
description = "Every check CI runs for services/collector-vpoint-pay."
depends = ["//services/collector-vpoint-pay:typecheck", "//services/collector-vpoint-pay:test"]
