# ref: https://mise.jdx.dev/tasks/
#:schema https://mise.jdx.dev/schema/mise.json

[tasks."types"]
description = "Generate the Cloudflare Worker types of services/collector-vpass."
run = "./node_modules/.bin/wrangler types"

[tasks."typecheck"]
description = "Type-check services/collector-vpass."
depends = ["//services/collector-vpass:types"]
run = "./node_modules/.bin/tsc --noEmit"

[tasks."test"]
description = "Run the services/collector-vpass tests."
depends = ["//services/collector-vpass:types"]
run = "bun test"

[tasks."dry-run"]
description = "Validate the services/collector-vpass deployment without uploading it."
depends = ["//services/collector-vpass:types"]
run = [
	"./node_modules/.bin/wrangler deploy --dry-run",
]

[tasks."bundle"]
description = "Emit the deployable bundle of services/collector-vpass for the release manifest."
depends = ["//services/collector-vpass:types"]
run = "./node_modules/.bin/wrangler deploy --dry-run --outdir ../../dist/vpass-json --config wrangler.jsonc"

[tasks."ci"]
description = "Every check CI runs for services/collector-vpass."
depends = ["//services/collector-vpass:typecheck", "//services/collector-vpass:test"]
