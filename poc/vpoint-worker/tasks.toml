# ref: https://mise.jdx.dev/tasks/
#:schema https://mise.jdx.dev/schema/mise-task.json

["vpoint-worker:types"]
description = "Generate the Cloudflare Worker types of poc/vpoint-worker."
dir = "poc/vpoint-worker"
run = "./node_modules/.bin/wrangler types"

["vpoint-worker:typecheck"]
description = "Type-check poc/vpoint-worker."
depends = ["vpoint-worker:types"]
dir = "poc/vpoint-worker"
run = "./node_modules/.bin/tsc --noEmit"

["vpoint-worker:test"]
description = "Run the poc/vpoint-worker tests."
depends = ["vpoint-worker:types"]
dir = "poc/vpoint-worker"
run = "bun test"

["vpoint-worker:dry-run"]
description = "Validate the poc/vpoint-worker deployment without uploading it."
depends = ["vpoint-worker:types"]
dir = "poc/vpoint-worker"
run = [
	"./node_modules/.bin/wrangler deploy --dry-run",
]

["ci:vpoint-worker"]
description = "Every check CI runs for poc/vpoint-worker."
depends = ["vpoint-worker:typecheck", "vpoint-worker:test"]
