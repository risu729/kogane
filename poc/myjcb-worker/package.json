{
  "name": "@kogane/poc-myjcb-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "wrangler types && tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json",
    "cf:types": "wrangler types",
    "cf:check": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy",
    "bw:passkey": "bun run scripts/sync-bitwarden-passkey.ts"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "1.4.0",
    "parse5": "8.0.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.8",
    "typescript": "5.9.3",
    "wrangler": "4.127.1"
  }
}
