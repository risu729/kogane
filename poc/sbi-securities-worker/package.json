{
  "name": "@kogane/poc-sbi-securities-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "wrangler types && tsc --noEmit",
    "cf:types": "wrangler types",
    "cf:check": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/bun": "1.3.8",
    "@types/node": "24.3.0",
    "typescript": "5.9.3",
    "wrangler": "4.126.0"
  }
}
