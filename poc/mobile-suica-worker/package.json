{
  "name": "@kogane/poc-mobile-suica-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "wrangler types && tsc --noEmit",
    "cf:types": "wrangler types",
    "cf:check": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy",
    "bw:verify": "node scripts/verify-bitwarden-passkey.mjs",
    "bw:sync": "node scripts/sync-bitwarden-passkey.mjs"
  },
  "dependencies": {
    "iconv-lite": "0.7.0"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "1.1.0",
    "@types/bun": "1.3.8",
    "@types/node": "24.3.0",
    "typescript": "5.9.3",
    "wrangler": "4.127.1"
  }
}
